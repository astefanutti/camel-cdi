<routes id="routes" xmlns="http://camel.apache.org/schema/spring">
    <route id="xml-route">
        <from uri="timer://foo?fixedRate=true&amp;period=2s"/>
        <to uri="bean:myBean?method=someMethodName"/>
        <log message="Date is ${body}"/>
    </route>
</routes>
